/**
 * Store configuration for app.
 *
 * Makes global object patAppConfig that stores any per instance settings/keys.
 */

 /*exported patAppConfig */
var patAppConfig = {
  // If was doing a production site, would need own keyl this is a test key.
  giphyKey: 'dc6zaTOxFJmzC'
};